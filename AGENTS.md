# Experiment 05 — Context Only

Before working on a Jobiss request, read `experiment-context/jobiss-brief.md` and use it only as product context.

This branch intentionally provides no additional design workflow, evaluation framework, anti-pattern library, memory, art direction, reference site, or aesthetic rules.

Use your normal professional judgment for design and implementation. Preserve existing required functionality unless the user explicitly asks to change it.

The user's explicit instructions take precedence.
