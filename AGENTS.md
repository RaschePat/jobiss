# Experiment 02 — B: Design Learning Memory

This branch is the treatment condition for Experiment 02.

Before working on Jobiss:
1. Read `experiment-context/jobiss-brief.md` as the common product context.
2. Read `memory/design-learning.md` as accumulated evidence from prior user judgments.
3. Use the professional frontend/design capabilities and skills normally available to Codex.

Memory behavior:
- Apply remembered judgments only when relevant to the current task.
- Translate preferences into current design decisions; do not recreate a previous page.
- Preserve product meaning and the user's current explicit request over remembered preferences.
- Keep this context orchestration behind the interface so an ordinary short user request is sufficient.

Experiment rules:
- Do not look at prior Experiment 01 / 01.5 outputs as visual references.
- Do not ask the user to restate preferences already represented in the memory file unless execution is genuinely blocked.
- Do not mention the memory file or experiment condition unless asked.
- Render and inspect the result before delivery when the environment allows it.
- The user's explicit instructions take precedence over this file.